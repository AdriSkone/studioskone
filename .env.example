# Copiez ce fichier en .env.local et remplissez les valeurs.
# .env.local est ignoré par git (via *.local dans .gitignore).
#
# En production, ces variables sont définies dans le dashboard Vercel
# (Project Settings → Environment Variables).

# ─────────────────────────────────────────────────────────────────────────────
# FRONT — exposé au navigateur (toutes les VITE_* sont injectées dans le bundle public).
# Ne JAMAIS y mettre de secret.
# ─────────────────────────────────────────────────────────────────────────────

# Umami : script de tracking chargé par src/main.ts et src/projet-page.ts.
# URL du script + identifiant du site (publics par design).
# Les stats se consultent directement sur le dashboard Umami Cloud natif
# (/admin y redirige). L'accès API stats est réservé au plan Pro → pas de token serveur ici.
VITE_UMAMI_SCRIPT_URL=https://cloud.umami.is
VITE_UMAMI_WEBSITE_ID=
